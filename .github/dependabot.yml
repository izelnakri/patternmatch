version: 2
updates:
  # Maintain dependencies for GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "daily"

  # Maintain npm devDependencies (no runtime deps to track)
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "daily"
